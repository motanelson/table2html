game secreat agent in this mission 3 jim you must lock the .class file to be run be others and only run in the new virtual machine , like this only you can run the .class file , only by rename function main into start


openjdk-asmtools-jasm Hello.jasm -w .

sudo dnf install openjdk-asmtools.noarch

/usr/bin/openjdk-asmtools-jdis -w . Hello.class
